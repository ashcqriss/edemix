# UI prototypes

The seven prototype attachments referenced in `docs/DESIGN.md` —
`main_homescreen`, `dock`, `panelv`, `mobileratioqo`, `mobile_dock_ratio_2`,
`login_screen`, `on/off2` — are **NOT currently committed** to this repo.
They exist only in the planning session.

When the design pass starts, the user should re-attach the original PNG/SVG
files and commit them here so the brief and source images live together.
