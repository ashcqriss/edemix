# Edemint per-user capability settings. This file is sourced after the base
# Hyprland configuration so Settings can persist input and motion preferences.
input {
    kb_layout = us
}
animations {
    enabled = false
}
