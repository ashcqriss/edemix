# Edemint Hyprland effects profile.
# Set $low_power = false here to enable animations / rounding / blur / shadows
# on capable hardware. Defaults below match the low-power profile so this file
# is safe to ship empty/edited.

# $low_power = false
