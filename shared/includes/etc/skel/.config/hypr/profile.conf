# Edemint Hyprland profile.
#
# These values mirror docs/PALETTE.md and may be overridden by hardware- or
# user-specific profiles. Keep expensive effects disabled on the base image.

$ed_background = rgba(162d13ff)
$ed_surface = rgba(253c22ff)
$ed_primary = rgba(2001ffff)
$ed_primary_soft = rgba(6d58ffff)
$ed_accent = rgba(45e289ff)
$ed_focus = rgba(74fbeaff)

# Capable hardware may opt in and add effect overrides below.
# $low_power = false
